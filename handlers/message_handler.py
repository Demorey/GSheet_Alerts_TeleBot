from create_bot import dp, bot
from aiogram import types


# @dp.message_handler(commands='start')
# async def command_start(message: types.Message):
    # print(message)
    # await bot.send_message(, f'id группы: {message.chat.id}')
